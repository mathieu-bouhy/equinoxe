import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { ApiError, api } from '../services/api'; import { Button, Card, Input } from '../components/ui';
export function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false),nav=useNavigate();return <main className="login"><Card className="login-card"><div className="brand centered"><span className="sun">✦</span>Equinoxe</div><h1>Bienvenue</h1><p>Connectez-vous à votre espace de pilotage.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{await api.login(email,password);nav('/');}catch(reason){setError(reason instanceof ApiError&&(reason.status===0||reason.status>=500)?'Le serveur local est indisponible. Relancez Equinoxe puis réessayez.':'Identifiants incorrects.')}finally{setBusy(false)}}}><label>Email<Input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Mot de passe<Input type="password" minLength={1} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="form-error">{error}</div>}<Button disabled={busy}>{busy?'Connexion…':'Se connecter'}</Button></form></Card></main>}
