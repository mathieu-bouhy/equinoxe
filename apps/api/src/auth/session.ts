import type { Role } from '@equinoxe/shared';
const encoder=new TextEncoder(); const b64=(v:Uint8Array)=>btoa(String.fromCharCode(...v)).replaceAll('+','-').replaceAll('/','_').replaceAll('=',''); const bytes=(v:string)=>Uint8Array.from(atob(v.replaceAll('-','+').replaceAll('_','/')),c=>c.charCodeAt(0));
export type Session={sub:string; role:Role; exp:number};
export async function sign(session:Session, secret:string){ const body=b64(encoder.encode(JSON.stringify(session))); const key=await crypto.subtle.importKey('raw',encoder.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']); return `${body}.${b64(new Uint8Array(await crypto.subtle.sign('HMAC',key,encoder.encode(body))))}`; }
export async function verify(token:string|undefined,secret:string):Promise<Session|null>{ try { if(!token) return null; const [body,signature]=token.split('.'); if(!body||!signature)return null; const key=await crypto.subtle.importKey('raw',encoder.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['verify']); if(!await crypto.subtle.verify('HMAC',key,bytes(signature),encoder.encode(body)))return null; const parsed=JSON.parse(new TextDecoder().decode(bytes(body))) as Session; return parsed.exp>Date.now()?parsed:null; }catch{return null;} }
export const parseCookies=(request:Request)=>Object.fromEntries((request.headers.get('cookie')??'').split(';').filter(Boolean).map(x=>x.trim().split('=')));
